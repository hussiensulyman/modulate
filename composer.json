{
  "name": "hussiensulyman/modulate",
  "description": "A modular monolith architecture toolkit for Laravel.",
  "type": "library",
  "license": "MIT",
  "require": {
    "php": "^8.2",
    "illuminate/support": "^10.0|^11.0",
    "symfony/finder": "^6.0|^7.0"
  },
  "require-dev": {
    "orchestra/testbench": "^8.0|^9.0",
    "pestphp/pest": "^2.0|^3.0"
  },
  "autoload": {
    "psr-4": {
      "Modulate\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Tests\\": "tests/"
    }
  },
  "minimum-stability": "dev",
  "prefer-stable": true,
  "extra": {
    "laravel": {
      "providers": [
        "Modulate\\ModulateServiceProvider"
      ]
    }
  },
  "scripts": {
    "test": "pest",
    "coverage:check": "php -r \"if (!extension_loaded('xdebug') && !extension_loaded('pcov')) {fwrite(STDERR, 'No code coverage driver loaded. Install/enable Xdebug or PCOV.'.PHP_EOL); exit(1);} echo 'Coverage driver available.'.PHP_EOL;\"",
    "test:ci": "pest --coverage-clover=coverage.xml",
    "lint": "pest --filter ViolationScanner"
  },
  "config": {
    "allow-plugins": {
      "pestphp/pest-plugin": true
    }
  }
}
